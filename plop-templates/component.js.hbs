import BaseComponent from "../../utils/base-component";

export default class {{camelCase name}} extends BaseComponent {
    constructor(selector) {
        super(selector);
    }

    init(options) {
        this.addEventListeners({});
        console.log("{{camelCase name}} is ready!")
    }
}